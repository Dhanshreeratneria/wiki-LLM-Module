# Threads

Auto-saved conversation logs from Cruz Brain queries. Each file is one
thread (one day, one user, one topic). New turns are appended; the file is
never rewritten.

File naming: `{user-name}-{thread-name}-{YYYY-MM-DD}.md`

These are LLM-managed; safe to delete or archive old threads.
